"use client";
import { createBrowserClient } from "@supabase/ssr";import { useRouter } from "next/navigation";import { useState } from "react";import { Logo } from "./logo";
export function AdminLoginForm(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function login(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget);const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setError('Supabase Auth тохиргоо хийгдээгүй байна. .env.local файлыг тохируулна уу.');setBusy(false);return}const supabase=createBrowserClient(url,key);const {error}=await supabase.auth.signInWithPassword({email:String(data.get('email')),password:String(data.get('password'))});if(error){setError('Имэйл эсвэл нууц үг буруу байна.');setBusy(false);return}router.push('/admin');router.refresh()}return <form className="login-form" onSubmit={login}><Logo/><h2>Админ нэвтрэх</h2><p className="section-copy">Эрх бүхий байгууллагын бүртгэлээр нэвтэрнэ үү.</p><div className="field"><label>Имэйл</label><input name="email" type="email" required placeholder="name@noyonhouse.mn"/></div><div className="field"><label>Нууц үг</label><input name="password" type="password" required/></div>{error&&<p style={{color:'#b51f2b',fontSize:13}}>{error}</p>}<button className="button" style={{width:'100%'}} disabled={busy}>{busy?'Нэвтэрч байна…':'Нэвтрэх'}</button></form>}
