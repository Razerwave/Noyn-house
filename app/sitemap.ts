import type { MetadataRoute } from "next";import { models,projects } from "@/lib/data";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://noyonhouse.mn';const routes=['','/houses','/projects','/technology','/services','/process','/about','/faq','/news','/contact','/quote','/privacy','/terms'];return [...routes.map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:url===''?1:.7})),...models.map(m=>({url:`${base}/houses/${m.slug}`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:.8})),...projects.map(p=>({url:`${base}/projects/${p.slug}`,lastModified:new Date(),changeFrequency:'monthly' as const,priority:.7}))]}
