import { PageHero } from "@/components/page-hero";
import { getPublicTechnologyContent } from "@/lib/public-technology";
export const metadata={title:"Барилгын технологи"};
export const dynamic = "force-dynamic";
export default async function Technology(){const content = await getPublicTechnologyContent();return <><PageHero eyebrow={content.eyebrow} title={content.title} copy={content.copy}/><section className="section"><div className="container tech-stack"><div><div className="eyebrow">{content.wallEyebrow}</div><h2 className="section-title">{content.wallTitle}</h2><p className="section-copy">{content.wallCopy}</p></div><div className="wall">{content.layers.map((layer,index)=><div className="wall-layer" key={`${layer}-${index}`}><span>{layer}</span><b>{String(index+1).padStart(2,'0')}</b></div>)}</div></div></section><section className="section soft"><div className="container service-grid">{content.cards.map((card,index)=><article className="service-card" key={`${card.title}-${index}`}><span>{String(index+1).padStart(2,'0')}</span><h3>{card.title}</h3><p>{card.description}</p></article>)}</div></section></>}
